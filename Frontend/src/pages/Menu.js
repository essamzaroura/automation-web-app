import React from 'react';
import { Box, Typography } from '@mui/material';

const MenuPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">Menu Page</Typography>
      <Typography mt={1}>Here you can add user options, settings, or preferences.</Typography>
    </Box>
  );
};

export default MenuPage;
